[build]
  command = "npm run build"
  publish = "dist"

# Must precede the SPA catch-all: rules are evaluated in order.
[[redirects]]
  from = "/gravlang"
  to = "/gravlang/"
  status = 301

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
